apiVersion: v1
kind: Service
metadata:
  name: {{ include "linguagap.fullname" . }}
  labels:
    {{- include "linguagap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "linguagap.selectorLabels" . | nindent 4 }}
